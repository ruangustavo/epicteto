{
  "name": "epicteto",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "lint": "oxlint",
    "fmt": "oxlint --fix; oxfmt",
    "check": "bun run typecheck && bun run lint && oxfmt --check"
  },
  "dependencies": {
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@oxlint/plugins": "1.82.0",
    "@stylistic/eslint-plugin": "^5.10.0",
    "bun-types": "latest",
    "oxfmt": "^0.67.0",
    "oxlint": "1.82.0",
    "oxlint-readable": "github:ruangustavo/oxlint-readable",
    "typescript": "^5.6.0"
  }
}
