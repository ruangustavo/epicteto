Replaying the verification scripts failed. Fix them following the `verify-ui` skill, re-run both until `sh /state/boot.sh && sh /state/verify.sh` exits 0, then run `agent-browser close`.

Exit code: {{exit_code}}

Output (tail):
```
{{output}}
```

Files changed on this branch since the scripts were written:
{{files}}

Do not commit anything in this step.
