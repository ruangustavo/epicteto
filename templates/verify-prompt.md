The implementation for issue #{{issue}} is done and all checks pass. Now produce the verification scripts.

Load and follow the `verify-ui` skill exactly. The issue, for the acceptance criteria to demonstrate:

# Issue #{{issue}}: {{title}}

{{body}}

Files changed on this branch:
{{files}}

Deliverables: `/state/boot.sh`, `/state/verify.sh`, `/state/verify-result.md`. Both scripts must have run successfully at least once before you finish. Do not commit anything in this step.
